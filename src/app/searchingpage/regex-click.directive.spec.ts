import { RegexClickDirective } from './regex-click.directive';

describe('RegexClickDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexClickDirective();
    expect(directive).toBeTruthy();
  });
});
